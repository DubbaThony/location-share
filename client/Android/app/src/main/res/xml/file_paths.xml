<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Maps `<files-dir>/*` (i.e. context.filesDir) to a content:// URI. The
         debug-log exporter writes the rolled-up log here as debug-export.log
         and hands the resulting URI to ACTION_SEND. -->
    <files-path name="debug" path="." />
</paths>
